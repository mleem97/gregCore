name: Generate FrameworkAPI Docs

on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - 'game_hooks.json'
      - 'framework/greg_hooks.json'

permissions:
  contents: write

jobs:
  generate-docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate FrameworkAPI.md
        run: |
          HOOK_ARGS="--game-hooks game_hooks.json"
          if [ -f "framework/greg_hooks.json" ]; then HOOK_ARGS="$HOOK_ARGS --greg-hooks framework/greg_hooks.json"; fi
          python3 scripts/generate_api_docs.py $HOOK_ARGS \
            --output docs/FrameworkAPI.md \
            --version "$(cat VERSION | tr -d '[:space:]')"

      - name: Commit updated docs
        run: |
          if [[ -n "$(git status --porcelain docs/FrameworkAPI.md)" ]]; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add docs/FrameworkAPI.md
            git commit -m "docs: regenerate FrameworkAPI from hook definitions [skip ci]"
            git push
          else
            echo "FrameworkAPI.md unchanged – skipping commit."
          fi

