# Plugin template

Same idea as [`templates/mod/`](../mod/README.md), but for **FFM.Plugin.*** style assemblies loaded by the framework.

- `src/` — plugin source.
- `fmf/hooks.json` — hooks used or exposed.
- `workshop/` — optional; many plugins ship with the main game download instead of Workshop.
