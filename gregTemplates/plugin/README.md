# Plugin template

Same idea as [`templates/mod/`](../mod/README.md), but for **greg.Plugin.*** style assemblies loaded by the framework.

- `src/` — plugin source.
- `greg/hooks.json` — hooks used or exposed.
- `workshop/` — optional; many plugins ship with the main game download instead of Workshop.
