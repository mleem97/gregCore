1. **Analyze CI failures:** The CI build job failed on `ubuntu-latest` (and `windows-latest` was canceled). The error is: `cp: cannot stat 'framework/greg_hooks.json': No such file or directory`.
2. **Review memory:** The memory explicitly states: "In gregCore's GitHub Actions workflows (e.g., `build.yml`), artifact files like `framework/greg_hooks.json` may not always be present depending on the build context. Always wrap `cp` commands for such files in existence checks (e.g., `if [ -f "file" ]; then cp ...; fi`) to prevent CI job failures."
3. **Fix `build.yml`:** Edit `.github/workflows/build.yml` using `replace_with_git_merge_diff` or a Python script via `run_in_bash_session` to wrap `cp framework/greg_hooks.json` in `if [ -f "framework/greg_hooks.json" ]; then cp framework/greg_hooks.json "$OUT/Mods/"; fi` and `if [ -f "framework/greg_hooks.json" ]; then cp framework/greg_hooks.json "$OUT/BepInEx/plugins/gregCore/"; fi` in both the MelonLoader and BepInEx build steps.
4. **Submit PR:** Since this is a CI pipeline fix directly requested by a failure, use `submit` tool to finalize the change, referencing the CI fix.
