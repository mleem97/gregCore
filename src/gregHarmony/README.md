Optional: run `gregCore/scripts/Generate-GregHooksFromIl2CppDump.ps1` to emit domain Harmony stubs (`Greg*Hooks.cs`) into this folder. The main plugin does not require them for `GregHookIntegration` (FFI → `greg.*`).

